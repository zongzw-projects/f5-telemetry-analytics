<source>
  @type tcp
  tag tcp.source
  port 20002

  @log_level error
  <parse>
    @type regexp
    # May 22 04:56:26 F5-LTM-7000-OAEBANK-01 err tmm3[24144]: 01230140:3: RST sent from 10.235.103.84:1171 to 10.235.99.238:51228, [0x2b1ea6d:974] RST from BIG-IP internal Linux host
    expression /^(?<timestamp>\w{3}\s+\d+ (\d{2}:){2}\d\d) (?<hostname>\S+) (?<log_level>\w+) (?<process_name>\w+)\[(?<process_id>\d+)\]:.*?RST sent from (?<src_addr>\S+):(?<src_port>\d+) to (?<dst_addr>\S+):(?<dst_port>\d+), .*? (?<rst_from_type>.*)$/
  </parse>

  @label @KAFKA
</source>
