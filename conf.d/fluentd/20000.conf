<source>
  @type tcp
  tag tcpevents # required
  <parse>
    @type json
  </parse>
  port 20000   # optional. 5170 by default
  bind 0.0.0.0 # optional. 0.0.0.0 by default
</source>

<match tcpevents>
  @type stdout
</match>
